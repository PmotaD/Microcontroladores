#include < stdio.h>
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "driver/gpio.h"

#define LED 2
#define BT 17

void app_mainO
{

gpio set direction(LED, GPIO_ MODE__OUTPUT):
gpio_set _direction(BT, GPIQ_MODE_INPUT);

gpio_set_pull_mode(BT, GRIO_ PULLP_ONLY;

 while (1)
 {
  
  int satate_bt = gpio_get_level(BT);
  printf("%d \n", state_bt);
  if (!state_bt){
  gpio_set_level(LED,1);
  }

  else{
  gpio_set_level(LED,0);
  }

  vTaskDelay(200 / portTICK_RATE_MS);

 }
}